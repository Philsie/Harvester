

{
    "comment-refresh_delay": "FroTime the eventlet sleeps after finishing a pass",
    "refresh_delay": 0.05,

    "comment-logEventlet": "Turn On/Off logging of actions happening inside the eventlet (True/False)--> can cause spam inside the log",
    "logEventlet": "False",

    "comment-logSignOfLife": "log a sign of Life every x seconds, set to 0 to turn off",
    "logSignOfLife": 10,

    "comment-port": "The port the frontend will be running on",
    "port": "5050",

    "comment-colorstring": "Use the package colorstring True/False",
    "colorstring": "True"
}
